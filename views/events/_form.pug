.form-group
  label(for="title") Title
  input.form-control(type="text", name="title", placeholder="이벤트 제목을 작성해주세요!", value=event.title)
.form-group
  label(for="event") 어떤 이벤트인가요?
  textarea.form-control(name="content", rows="5")= event.content
.form-group
  label(for="tags") Tags
  input.form-control(type="text", name="tags", placeholder="분류 태그를 작성해주세요!", value=event.tags ? event.tags.join(" ") : "")
